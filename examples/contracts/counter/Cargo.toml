[package]
name = "counter-contract"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[profile.release]
panic = "abort"

[dependencies]
blocky-sdk = { path = "../../../blocky-sdk" }
